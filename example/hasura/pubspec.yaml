name: hasura_example
version: 0.0.1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  http:
  gql_websocket_link:

dev_dependencies:
  test:
  lints: ^6.0.0
  build_runner:
  json_serializable:
  artemis:
    path: ../../.
