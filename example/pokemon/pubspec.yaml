name: pokemon_example
version: 0.0.1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  http:

dev_dependencies:
  test:
  build_runner:
  json_serializable:
  lints: ^6.0.0
  artemis:
    path: ../../.
